<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Planetze35 - login"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Planetze35"
        tools:targetApi="31">
        <activity
            android:name=".EcoGaugeActivity"
            android:exported="true"
            android:label="Eco Gauge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EmailVerificationPageActivity"
            android:exported="false"
            android:label="Verify you email" />
        <activity
            android:name=".SignupPageActivity"
            android:exported="false"
            android:label="Planetze35 - signup"
            android:parentActivityName=".LoginPageActivity" />
        <activity
            android:name=".LoginPageActivity"
            android:exported="false"
            android:theme="@style/Theme.Planetze35.Actionbar">

        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>